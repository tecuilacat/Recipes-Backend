dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("libs.versions.toml"))
        }
    }
}

rootProject.name = 'Recipes-Backend'
include 'app'
include 'recipe'